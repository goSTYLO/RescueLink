# Deep Learning & ML
# NOTE: install PyTorch separately to match your CUDA runtime.
# For CUDA 12.8 (cu128) install the matching wheels using the PyTorch index:
#   python -m pip install --upgrade pip
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# For CPU-only installs use the cpu wheels:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# (Do NOT use default pip install torch - it may pull cu118 which conflicts with CUDA 12.)
transformers>=4.35.0
scikit-learn>=1.3.0
datasets>=2.14.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.1
sounddevice>=0.4.6
# ctranslate2>=4.6.3 adds CUDA 12.8 support; cuDNN optional for Whisper Conv1d
ctranslate2>=4.6.3
faster-whisper>=1.0.3
# CUDA 12 libs for PyTorch + faster-whisper (cublas64_12.dll, nvrtc for caffe2_nvrtc.dll)
nvidia-cublas-cu12
nvidia-cuda-nvrtc-cu12

# API & Server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# HuggingFace Integration (UPDATED)
huggingface_hub>=0.18.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
imageio-ffmpeg>=0.6.0

# Visualization (optional, for notebooks)
matplotlib>=3.8.0
seaborn>=0.13.0

# Jupyter (for testing notebooks)
jupyter>=1.0.0
ipykernel>=6.26.0

# Testing
pytest>=8.0.0

